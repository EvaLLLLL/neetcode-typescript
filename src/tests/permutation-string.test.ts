import { permutationString } from '../problems/permutation-string';

describe('permutation-string', () => {
    test('case 1', () => {
        // TODO: Add test cases
        expect(permutationString()).toBe();
    });
});