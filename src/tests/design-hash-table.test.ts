import { designHashTable } from '../problems/design-hash-table';

describe('design-hash-table', () => {
    test('case 1', () => {
        // TODO: Add test cases
        expect(designHashTable()).toBe();
    });
});